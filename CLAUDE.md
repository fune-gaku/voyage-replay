# CLAUDE.md — voyage-replay

AI コーディングエージェント向けの作業ノート。「何のツールか」「使い方」は **README.md**
にあるので、ここに書いていないことはそちらを読むこと。

## スタックとパッケージマネージャ

- TypeScript / Vite / Vitest / ESLint / Prettier。3D は **three.js**（`src/render/`）
- パッケージマネージャは **npm**。`npm install <pkg>` を使い、package.json を手で書き換えない。
  yarn ではない（OSS なので Node 同梱の npm で追加インストール不要にする、
  yarn classic はメンテナンスモード、という理由）

## 変更したら走らせるもの

```
npm run check:config  # lint と tsc が「見る設定になっているか」を検査（後述）
npm run lint          # ESLint（型情報つき）。関数の大きさの上限もここ
npm run typecheck     # tsc --noEmit（src / test / *.config.ts を全部見る）
npm test              # Vitest + カバレッジ。閾値を割ると落ちる（後述）
npm run build         # typecheck + vite build
npm run format        # Prettier。.prettierignore で *.md は対象外
npm run dev           # 開発サーバ（http://localhost:5173/）
npm run build:single -- <scenario.voyage.json>   # 自己完結 HTML 1枚を dist/ に出す
```

`build:single` は「見るたびに dev サーバを回す」を不要にするためのもの。通常の
`npm run build` が吐く `dist/index.html` は JS を別ファイルから読み、シナリオを `fetch`
するので **`file://` では開けない**。1枚に畳めば、ダブルクリックで開き、メールに添付でき、
記事に埋め込め、何年後でも開く（ページが動くために取りに行くものが残っていないため。
地図タイルだけは実行時取得で、来なくても再現はそのまま動く——後述）。

**実装上の罠**: `String.prototype.replace` の**置換文字列**は `$&` / `` $` `` / `$'` / `$1`
を展開する。ミニファイされた JS にはこれらが普通に含まれるので、バンドルを置換文字列として
渡すと `$'` が「マッチ以降の全文」に化けて HTML が壊れる。**置換は必ず関数で渡すこと**
（`scripts/build-single.mjs` にコメントあり）。出力に外部参照が残っていないかは同スクリプトが
検査して、残っていれば失敗する。

CI は同じものを順に流すだけ。ローカルで通れば CI も通る。

## 認証情報（gitleaks）

2層ある。

- **コミット前**: `.githooks/pre-commit` が staged 差分を走査する。**`.git/hooks` ではなく
  git 追跡下に置いてある**ので、PR でレビューでき、どのマシンでも同一になる。
  `npm install` の `prepare` が `core.hooksPath` を `.githooks` に向けるので、
  手でやることは `brew install gitleaks` だけ
- **push 後**: CI が **全履歴 + 作業ツリー**を走査する。ただしこれは最後の砦で、
  CI が動く頃には認証情報は既にサーバ上にある

```
npm run check:secrets   # 手動で履歴と作業ツリーを走査
```

誤検知は `.gitleaksignore` にフィンガープリントを、理由コメント付きで追加する。

**負のテストをするときは AWS の例示キー `AKIAIOSFODNN7EXAMPLE` を使わないこと。**
gitleaks が公式ドキュメントの例として許可リストに入れているので検知されず、
「フックが壊れている」という誤った結論に至る（一度踏んだ）。`ghp_` 形式の GitHub PAT か
`-----BEGIN RSA PRIVATE KEY-----` を使うと確実に発火する。

本物を commit してしまったら、**まずローテーションする**。履歴を書き換えても公開は取り消せない。

## 設定は「読む」のではなく「実効値を出して数える」

**入っていない設定は何も報告しない。** これがこのレポで一度踏んだ罠で、最初の版は
`tseslint.configs.recommended` + `strict: true` という、いかにも厳しそうで実際には
**型情報ルールが1本も動いていない**設定だった（`parserOptions.projectService` が無いので
そもそも型を見られない）。lint は通り、CI は緑で、欠けていること自体はどこにも出なかった。

そのため `npm run check:config` が、**設定ファイルではなく実効値**を読んで、
必要なルールとコンパイラフラグが実際に有効かを検査する。CI では lint より先に走る。

設定を触ったら、この3つで確かめること。

```
npx eslint --print-config src/core/track.ts   # そのファイルに実際に効いているルール
npx tsc -p tsconfig.json --showConfig         # extends 解決後のコンパイラ設定
npx tsc -p tsconfig.json --noEmit --<flag>    # そのフラグを入れたときの違反件数
```

- **`strict: true` は「厳しい設定」ではない。** `noUncheckedIndexedAccess`,
  `exactOptionalPropertyTypes`, `noImplicitReturns`, `noPropertyAccessFromIndexSignature` などは
  含まれない。このレポは個別に入れてある
- **型情報ルールは `*TypeChecked` プリセットにしか無い。** `recommended` にも `strict` にも
  `no-floating-promises` も `no-unsafe-*` も `consistent-type-assertions` も入っていない
- **例外は `eslint.config.js` に理由付きで書く。** ファイルに散らした `eslint-disable` コメントは、
  気づかないうちにルールを失う経路そのもの

詳細と実測値は [`docs/verifying-config.md`](docs/verifying-config.md)。

## 関数の大きさとテストの閾値

**どちらも文章ではなく機械が落とす。** 書いておくだけの規約は、守られなくなったときに何も
言わないので、このレポでは両方 CI のゲートにしてある。

### 3つの上限（`eslint.config.js`、`npm run lint` で落ちる）

| ルール | 値 | ESLint 既定 | 何を捕まえるか |
|---|---:|---:|---|
| `max-lines-per-function` | 30 | 50 | 散らかった関数 |
| `complexity` | 10 | 20 | 分岐した関数 |
| `max-params` | 4 | 3 | 上2つが押し出した先 |

**3つが別のものを測っているのが要点で、行数だけでは足りない。**

- **コメントと空行は数えない。** このレポは「素直な実装だと何が間違うか」を長々と書く方針で、
  コメントを数える設定はその説明を消すことで支払わせる
- **`test/**` では行数だけ off。** ルールは `describe` のコールバックと関数を区別できない。
  `describe` は「何を検査しているか」の一覧であって処理ではなく、行数で割ると *主題* ではなく
  *長さ* でスイートが割れる。テスト側で効かせたいのは「1つの `it` が1つのことを主張する」で、
  これは数えられる性質ではないのでレビューで見る
- **`max-statements` は入れていない。** 分岐もネストも測らないので、一直線の代入14個と
  ネストした藪を同じ点数にする（`main` が前者）。分野が収束したのは複雑度のほうなので
  そちらを入れてある
- **`max-params` が鳴ったとき「引数を減らす」のはたいてい間違い。** 一緒に動く引数に名前を
  与える。`core/plausibility.ts` の `Interval`、`ui/panels.ts` の `Encounter`、
  `main.ts` の `Report` はすべてこのルールから出てきた

**30 であって 20 ではない理由（実測）。** 最初は20だった。本当に読みにくかったのは
47〜68行の関数（`checkPlausibility` 68、`wireControls` 66、`buildScene` 50）で、**30でも
全部捕まる**。20〜30の帯で捕まったのは複雑さではなく Prettier の折り返しだった
——`lampPosition` は **20行だが2文**で、Rule 21 の灯火ごとに tuple を返す `switch` にすぎない。
そして20を守るために **5引数の関数が4つ**生まれた（それ以前はゼロ）。これは複雑さが減ったのでは
なく呼び出し側へ移っただけで、いま `max-params` が捕まえているのがそれ。同じコードの
循環的複雑度は最大8、ネスト深さは最大2で、圧力がかかっていたのは行数だけだった。

**上限を30に上げたとき、分割済みのコードは戻していない。** 書けてテストも通っていて読みにくく
もないので、天井を上げるのは今後の圧力を消すためであって、触り直す理由にはならない。

### カバレッジ

`vitest.config.ts` の `thresholds`。`npm test` がカバレッジ込みで走るので、
**別コマンドを覚えていなくても必ず効く**。実測と下限:

| | 実測 | 下限 |
|---|---:|---:|
| Lines | 99.67% | 99% |
| Statements | 98.18% | 97% |
| Functions | 100% | 99% |
| Branches | 91.38% | 90% |

- **効いているのは `coverage.include: ["src/**/*.ts"]`。** これが無いと「テストが読み込んだ
  ファイル」だけが分母になり、**テストが1本も無いファイルは 0% ではなく不在**になる。
  つまり未テストのコードを足すと数字が *上がる*
- **除外は `src/main.ts` 1本だけ、しかも実行39行（src の 6%）。** 残っているのは
  「要素を引く・シナリオを読む・部品に渡す」だけ。除外の理由はブラウザではなく
  **このレポ自身の構造**で、`void main()` が module のトップにあるので import した時点で
  走り、何も export されていない
- **`main.ts` の中身が正しくある必要が出たら、推論を頑張るのではなく測れる場所へ動かす。**
  実際そうした: 録画ボタンは `src/ui/recording-button.ts` へ（同じ窓でバグが3件出たため）、
  再生コントロールは `src/ui/transport.ts` へ（`follower` の rAF ループが同じ形だったため。
  停止後もループが残ると画面は正常に見えたまま idle が二度と取れなくなる）
- **`player.ts` と `record.ts` を「実ブラウザが要る」として除外していたのは誤りだった。**
  検証せずに書いた主張で、実際には `record.ts` は `MediaRecorder` を1つ、`player.ts` は
  three.js の `WebGLRenderer` を1つスタブすれば素の Node で動く。両方いま lines 100%。
  除外していた間、**src の 44% が測定の外**にあり、報告していた数字は半分強のコードに
  対するものだった（現在は 94%）
- **原則: 除外は「測れないもの」に使う。「測っていないもの」に使わない。** 入れる前に
  安いスタンドインを試すこと。`test/record.spec.ts` `test/player.spec.ts` がその書き方で、
  スタンドイン不要な部分は `test/scene.spec.ts` `test/hull.spec.ts` `test/cameras.spec.ts`
- 閾値は「今の実測のすぐ下」に置いてある。**通らないから下げる、はしないこと。**
  実測が上がったら閾値も上げる

`npm run check:config` が全部を**実効値で**検査する（3つのルールが on か、値がまだ 30 / 10 / 4 か、
`skipComments` が生きているか、`thresholds` が消えていないか、`include` が `src/` を覆っているか）。
ルールを追加したら、一度その設定を壊して赤くなることを確認する。

## レイアウト

```
spec/       .voyage.json の JSON Schema。フォーマットの正本
src/core/   型・測地・水平線・時刻表示・天測・状況・検証・トラック標本化・物理スクリーニング・
            海面（波のスペクトル諸元）・遮蔽（波が視線を塞ぐ割合）
src/actors/vessel/  船に固有のもの（航海灯、アンテナ位置の補正、仮定した垂直寸法、将来は運動モデル）
src/render/ three.js。シーン・船体・航海灯・海の標識・カメラ・再生・録画・地図タイル・
            標高タイル・水面・波・曲率・画中キャプション
prototypes/ 紙の上では答えが出ない問いを動かして確かめる場所。typecheck と lint は効くが
            カバレッジは効かない。**実装になったら消す**（README.md にその規律がある）
src/ui/     画の下のテキストパネル、再生コントロール、録画ボタン
src/extract/  報告書などからシナリオを起こす（未実装）
examples/   参照事案 **1本だけ**。サンプルではなく回帰テスト。CI が schema 検証する
scenarios/  作業中の事案群。**git 追跡外**（README.md のみ追跡）
test/       Vitest
docs/       フォーマット仕様、ドメイン知識、抽出元ごとのメモ
plans/      1変更1設計メモ
```

**`examples/` と `scenarios/` を混同しないこと。** `examples/` の1本は
`test/examples.spec.ts` が最接近距離・方位の不動・灯火の見え方まで固定している回帰テストで、
「整理」のために消してはいけない。`scenarios/` は作業場で、追跡しないのは意図的
（データはコードと寿命が違い、出典と利用条件を各自背負い、一部は顧客のもの。MIT のツールの
履歴に入れるべきではない）。本数が増えたら専用レポへ移す。

dev サーバは `scenarios/` を配信し、かつ**監視する**（保存でページが再読込される）。
その仕掛けは `vite.config.ts` の `scenariosPlugin`。`publicDir` は追跡外ディレクトリを
見られないので、ミドルウェアで開けている。

`core/` と `actors/vessel/` を分けているのは、将来ジャンルを広げる余地を安く残すためで、
今すぐ汎用化する意図ではない。**`core/` に船の概念（航海灯・針路・喫水）を持ち込まないこと。**
逆に `actors/vessel/` は `core/` に依存してよい。

## 環境条件は3層に分ける

**`render/` は `environment`（生のシナリオフィールド）を読まないこと。** 読むのは
`core/conditions.ts` の `Conditions` だけ。

```
core/celestial.ts     時刻と場所 → 太陽・月の位置        純粋な算術
core/conditions.ts    ある瞬間の状況 ＋ 各値の出所        証拠
core/illumination.ts   条件 → どの天体・相対輝度・道の幅   物理
render/lighting.ts    放射量 → 光源とマテリアル           近似
```

海についても同じ形にしてある。

```
core/horizon.ts       眼高・相手の高さ・距離 → 視線のクリアランス   純粋な幾何
core/seaway.ts        報告値 → Hs・周期・スペクトル諸元 ＋ 出所      証拠
core/visibility.ts    2つを合わせて「隠れている割合」の区間          物理
render/waves.ts       スペクトル → 頂点変位と法線と空の反射            近似
```

理由: `buildScene(environment, ...)` が生のフィールドを直接読んで物理と見た目を同時に決めていた
結果が、二値の昼／夜だった。**変数を足すたびに `scene.ts` に特別扱いが1つ増える構造**で、
視界・雲量・うねり・岸の灯りを足していけば破綻する。

**`conditionsAt(at, environment, t)` は最初から時間の関数。** いまは大半が定数を返すだけだが、
太陽は既に定数ではない——参照事案は 87 分あり、**接触の 11 分前に航海薄明が終わる**。
定数に後から時間を入れるのは全消費者を触ること。

**条件は装飾ではなく証拠なので `core/` に置く。** レンダラでない消費者（パネルの視認性評価、
将来のレーダビュー）からも読める必要がある。

`illumination.ts` は **#37 で書いた**。境界だけ書いて空けてあったのは「中身が段階3にしか
無く、何も import しない空モジュールは腐るだけ」という理由で、水面が空を返すようになって
初めて段階3の消費者が現れた。いま入っているのは「どちらの天体が照らすか」「位相ごとの相対
輝度」「光の道の幅」で、**どれも比と幾何であって lux ではない**（雲を誰も書いていない）。

### 天測で間違えやすいこと

- **日没は高度 0° ではない。** 太陽中心が **−0.833°**（大気差 34′ ＋ 視半径 16′）。
  0° で計算すると約3分ずれ、COLREG 第20条「日没から日出まで」の議論では3分が効く
- **高度に大気差を足さないこと。** しきい値の側が既に含んでいる。両方入れると二重に数える
- **月には地平視差が要る。** 地表の観測者は地心の観測者より最大約 0.95° 低く見る。
  抜くと月出が数分早まり、しかも地平線近く——一番効く場所——で外れる
- **照輝面は明るさではない。** 満月は半月の約2倍ではなく**約10倍**（衝効果）。
  照輝面で光量を線形にスケールすると1桁近く外す
- **雲量が支配的で、報告書はまず書いていない。** 計算値はすべて上限。パネルにそう書いてある
- **`lightCondition` の enum は2つの軸を混ぜている。** day / night / twilight は太陽の話、
  restricted-visibility は視界の話で、霧の正午は両方。`Conditions` は分けてあり、
  比較できる軸だけ突き合わせる

**天測のテストは物理に当てること。** 自分の計算と突き合わせるテストは、実装が間違っていれば
同じだけ間違う。`test/celestial.spec.ts` は至の南中高度（90° − |緯度 − 傾斜角|）、
分の日出方位（真東）、満月は日没時に昇る、で固定している。

## このドメインで間違えやすいこと

ここが一番高くつく。順に、実際に踏んだ順。

### 対地針路（COG）と船首方位（HDG）は別物

COG は「どこへ進んでいるか」、HDG は「船首がどちらを向いているか」。差が偏角（drift angle）で、
潮流のある海域では目に見える大きさになる。そして**航海灯の照射範囲を決めるのは HDG のほう**。

`headingDegreesTrue` は頻繁に欠ける。簡易型（Class B）AIS は船首方位を送信しないので、
2隻の再現で片方に無いのが普通。**欠けているとき COG で黙って埋めないこと。** ライブラリは
`undefined` のまま返す。代用するなら呼び出し側が明示的にやり、画面にもそう出す
（`src/main.ts` の `aspects()` がその書き方）。

### 航海灯は「誰から見た相対方位か」を取り違えると 180° ずれる

`visibleLights(vessel, observerRelativeBearingDegrees)` の引数は
**灯火を掲げている船の船首を基準にした、観測者の方位**。逆にすると結果はちょうど 180° ずれ、
しかも「それらしく見える」ので、誰かが灯火から相手の見え方を読むまで気づかない。

照射範囲は COLREG 第21条。マスト灯 225°、舷灯 各112.5°、船尾灯 135°。225 + 135 = 360 で
水平線をちょうど覆うので、**どの方位にも必ず何かが見え、二重には数えない**。
`test/lights.spec.ts` がこれを 0.1° 刻みで固定している。

### 位置は GPS アンテナ。船体の基準点ではない

AIS が送るのはアンテナ位置で、JTSB の付表も脚注でそう書いている。180m 船ならアンテナは
船首から 100m 以上後ろにあるので、「アンテナ間 40m」は接触であってニアミスではない。
`track.positionAt` がどちらかを持ち、`vessel.referencePointOffsets` に AIS メッセージ5番の
4つの距離（船首・船尾・左舷・右舷まで）が入る。**この4つから全長と幅が逆算できる**ので、
報告書の諸元欄より正確なことがある（諸元欄の長さは登録長 Lr で、全長ではない）。

**そして向きを間違えやすい。** アンテナは船橋の上、船橋はたいてい船尾寄りにあるので、
**船体中心はアンテナより前方**にある。船体中心をアンテナに置いて描くと、船は後ろへずれる
（前ではない）。補正量は前方 `(船首 − 船尾) / 2`、右舷 `(右舷 − 左舷) / 2` で、
**4つの offsets の中だけで閉じる**（AIS は m 単位に丸めるので `beamMetres` と合わない）。
`src/actors/vessel/reference-point.ts` がこれを一手に引き受け、`render/player.ts` が
内側グループ1つに適用する。符号は `test/examples.spec.ts` が周防灘事案で固定していて、
**正しい符号でだけ2隻の船体が接触する**——逆にすると 21m 離れて衝突しなくなる。

**パネルの文は、描画側と同じ入力から組み立てること。** この補正を入れたとき、クロスレビューで
出た指摘の 6 件が「パネルが絵より強い主張をしている」だった（offsets を持つ＝補正した、と書く／
ゼロ変位を「移動した」と数える／ある一瞬の話をトラック全体から答える、など）。**言うことと
描くことを一致させる**のがこの変更の主題だったのに、幾何ではなく散文の側で破れていた。
`ui/panels.ts` の `isPlacedAt()` が `render/player.ts` の `placementOf()` と同じ問いに同じ答えを
出す形になっている。詳細は `plans/done/antenna-offset-6.md`。

**HDG も COG も無い標本では補正を当てないこと。** 補正は船首方位に沿って効くので、
向きが分からないまま当てると「真北へ 50m」のような架空の変位になる。船体は真北を向けて
*描く*しかない（何かは描く必要がある）が、*動かす*のは別で、動かせば報告位置——出典が
実際に言っている唯一のもの——から離れる。`player.ts` の `placementOf()` がこの判断を1か所で持ち、
船橋カメラも同じ判断を使う（片方だけ直すと目線が船体の外に出る）。

### 船体の形と寸法は 1 か所（#10）

**「最接近距離」は 2 つあり、別の時刻に起きる。** `core/track.ts` の
`closestPointOfApproach` は**報告位置どうし**＝アンテナ間で、これは報告書の付表と
突き合わせられる唯一の数字。船体間は `actors/vessel/separation.ts`。
参照事案では**アンテナ間 39.6 m / 18:13:35** に対し、**船体は 18:13:27.96 から 9.41 秒間接触**
——アンテナが一番近づく 7 秒前に既に当たっている。**「最接近の瞬間」と書くとき、どちらの
瞬間かを言うこと。**

- **矩形で距離を測らない。** issue #10 の本文は矩形を提案していたが、**描いている船体は
  矩形ではない**（船尾が絞られ、船首は二次曲線）。バウンディングボックスの角は
  傾斜した船首より**半幅ぶん**外に出るので、**絵が水を空けているところで接触したと言う**
  ——実測で接触 12 秒前に矩形 5.8 m 対 輪郭 8.2 m。
  **矩形は「内側かどうか」には使ってよい**（`test/examples.spec.ts` の符号テスト）。
  距離には使わない
- **形は `actors/vessel/hull-shape.ts` に 1 つ。** 以前は `render/hull.ts`（曲線・諸元）と
  `test/examples.spec.ts`（矩形・offsets）の 2 つがあって、形も寸法も食い違っていた。
  レンダラは点列から `Shape` を組み、距離は同じ点列を測る——**頂点まで同じ多角形**
- **寸法は 4 つの offsets の和を優先する。** 諸元欄の長さは登録長 Lr で全長ではない。
  タンカーは諸元 9.4 m 対 offsets 9.0 m で、**初接触が 1 秒動く**。AIS は m 単位に丸めるので
  offsets も粗いが、それでも measured のほう。**混ぜない**（長さを片方、幅を他方から取ると
  どちらの出典にも無い船になる）。`hullDimensions` が `from` で出所を返し、パネルが書く
  ——**船ごとに**（片方だけ offsets を持つ組は普通で、まとめて言うと片方の derivation が嘘になる）
- **「記載されている」と「使える」は別。** schema は 4 つの offset の下限を 0 にしているので、
  **寸法が届かず 0 のまま転記された船は正当なファイル**。和を取ると長さ 0・幅 0 の船体になり、
  何も描かれず、灯火は原点に行き、縮退した多角形に対して交差判定が**それらしい距離を返す**。
  どちらかの和が 0 なら諸元へ戻す。
  **そして「offsets があるか」を聞いている場所を全部その判定に繋ぐこと**——船橋位置も
  offsets の有無だけで決めていたので、全ゼロの船が**船体中央に「実測」の船橋**を持ち、
  同じページが「offsets は船体を測っていない」と書く状態になっていた（#10 のレビューで出た）
- **描かれる船体の寸法が変わったら、船体に載るものも一緒に動かす。** 舷灯は船側にあるので、
  諸元の幅でランプを置くと**船の横 0.2 m の海上に舷灯が浮く**。`render/navlights.ts` と
  甲板室も `hullDimensions` を通す
- **接触後は 0 を返し、貫入深さを返さない。** 輪郭は長さと幅から生成した「もっともらしい
  平面形」であって、その船の線図ではない。3 m 食い込んだと言えば、**この工具が発明した
  船首の 3 m** を報告することになる（曲率のところで船を曲げないのと同じ論拠）。
  形が支えられるのは「当たったか、いつか」なので、窓を返す
- **接触は「回数」で持つ。1 つの窓に畳まない。** 当たって離れてまた当たる（旋回中など）のは
  普通のことで、最初と最後だけを持つと**間の水面が空いていた区間まで接触と主張する**。
  `contacts: Contact[]`
- **窓の長さは「両端の差」であって「接触していた標本の個数」ではない。** 1 秒刻みで
  18:13:28〜18:13:37 は標本 10 個・経過 9 秒。`to - from + 1` と書いて 1 つずれた（#59 の
  レビューで指摘された）
- **端は探索の刻みではなく船体から取る。** 再生は連続なので、たまたま接触が見えた最初の
  1 秒を報告すると、ページが絵から最大 1 刻みずれる。標本間は直線補間なので**二分探索で
  正確に決まる**——参照事案で 18:13:27.961〜18:13:37.371。
  ただし**刻みより短い接触は取りこぼす**ので、刻みを持ち回ってページに書く
- **賢い探索より細かい探索。** 最接近を三分探索で詰めようとして指摘された——**回転する
  多角形どうしの最短距離は区分関数**で、どの頂点‐辺の組が最短かが途中で切り替わる。
  単峰ではないので、2 点の比較で半分を捨てると**深いほうの谷を捨てて浅いほうを報告しうる**。
  仮定の要らない細かい掃引にする（1 刻みを 256 分割）。**「滑らかで単峰」と書いたのが
  overclaim だった**
- **同じ仮定は 2 か所にある。片方だけ外すと残りが牙をむく。** 三分探索を外したあと、
  接触境界の二分探索に「区間内の遷移は 1 回」という同型の仮定が残っていた。しかもこちらは
  **接触を見落とすのではなく、離れていた時間を接触と報告する**（1 刻みの中で当たって離れて
  また当たると、間の水面をまたいで 1 つの窓になる）
- **粗い刻みを安全にするのは上界であって勘ではない。ただし上界の取り方を 2 回間違えた。**
  1 回目: 頂点の**端点間の弦**を移動量とした——並進と回転が同時に起きると各点は曲線を通るので、
  戻ってくる旋回は移動を端点から**丸ごと隠す**。正しくは「報告位置の直線距離 ＋ 自分の半径 ×
  角変位の弧」。2 回目: 上界は**標本をまたぐと成立しない**（行って戻れる）ので、
  **両船の標本時刻は刻みと無関係に必ず見る**
- **打ち切りの証明は「状態が変わるまでの余裕」で、接触中も要る。** 水があるなら間隔、
  重なっているなら**重なりの深さ**。深さが無いと接触中の区間が永久に discharge せず、
  再帰が全部 DEEPEST まで潜る（実測 130 秒 → 0.96 秒）。
  **深さは証明にだけ使い、ページには出さない**——「出さない」の論拠は
  「読者に何を主張してよいか」であって「内部で何を計算してよいか」ではない
- **固定分割は証明ではない。** 256 分割にしても、その 1/256 の中で当たって離れてまた当たれる。
  **上界が discharge するまで半分に割る**——ほとんどの区間は 1 回目で終わる（何マイルも離れている）
- **上界は「相対運動」で取る。絶対運動で取ると並走が全部「接触寸前」になる。** 北へ並んで
  進む 2 隻は何マイル動いても間隔が変わらない。実測: 絶対だと同一航跡のテストが**メモリ枯渇**
- **重なりの深さは頂点だけで測ると退化する。** 完全に重なった 2 つの輪郭では**全頂点が相手の
  境界上**にあって深さ 0 になり、証明が立たず永久に潜る。**重心も候補に入れる**
- **飛びは上界で抑えられない**（#12）。そこだけは discharge しないので、**時間の床**（1 ms）を
  宣言して打ち切る。**そして「有無」ではなく「どのフィールドから来たか」で判定する**——
  `HDG ?? COG` は、片方の標本にだけ HDG があると区間の中央で COG から HDG へ跳ぶのに、
  どちらの端にも「向き」はあるので有無だけでは検出できない
- **「区間内で状態が変わらない」証明は「区間内でより近づかない」証明ではない。** 両端 100 m で
  上界 90 m なら接触はしないが、途中で 10 m まで寄れる。最小値には**別の証明（branch and
  bound）**が要る——「その区間が持ちうる最小値 ＝ 小さいほうの端 − 上界、下限は 0」が
  既知の最良値以上なら見る必要が無い。**下限 0 を忘れると、接触後に全区間を床まで割る**
- **粗いパスを最後まで回してから細かくする。** bound は手持ちの値の分しか効かないので、
  左から順に細かくすると前半を「これから更新される値」の証明に費やす（実測 46 秒 → 4.3 秒）
- **ページに出す限界は刻みでも床でもなく「実際に残った区間」。** 証明が付くまで割るので、
  刻みを限界と書くと実際より 3 桁小さく報告する。かといって床を書くのも「探索についての主張」で
  あって「探索の報告」ではない。**証明できずに止まった最長区間を測って持ち回る**
- **浅い交差（grazing）には深さが無いので、証明が立たず永久に割る。** 両端で辺だけ交差して
  頂点も重心も相手の内側に無いと `overlapDepth` は 0。丸めた位置と生成した輪郭では珍しくない
  （横付け、衝突直後）ので、**細分に予算を付けて止め、残した量を報告する**
- **片方だけ断りを付けると、もう片方が確定値に見える。** 船体側に「刻み」「補間」と書いた
  結果、アンテナ間 CPA のほうが「出典が測った値」に見えるようになった。あちらも 1 秒格子で
  拾った直線補間上の点なので、同じ断りを付ける
- **接触の窓は補間の産物で、そう書く。** 参照事案の接触は、標本間隔 13 秒と 20 秒の
  直線補間区間の内側にある。「9.41 秒間接触」は出典ではなくこの工具の性質
- **配置の規則は `placementFor` に 1 つ。** HDG も COG も無い標本では offset を当てず
  真北を向けて描く（`plans/done/antenna-offset-6.md`）。距離の側で書き直すと、
  **絵が描いていない場所にいる船の距離**を答える

### 海上標識は種別で答えが変わる

浮標（buoy）と灯標・立標（beacon）は1つの表に並ぶが、ほとんど何も同じ意味ではない。
**種別で答えの変わるセルは、何を答えているかを毎回書くこと。**

- **`heightMetres` は両方とも「水面から上」。** 灯標の「基礎から上」（灯台表の塔高）は自然な
  読みだが**このフォーマットでは使えない**——基礎の水深が無いと上端を置けないので、
  与えられたレンダラは水深を発明するか datum を無視するかしかない。結果として
  **灯標は記載高さより高く描かれる**（水面下の基礎は構造に比例した作り物で、報告する高さには
  入れない）。パネルがそれを明記する。
  一度間違えた: schema とパネルが「基礎から」と書き、`render/mark.ts` は上端を**水面から**
  `heightMetres` に置いていた——6 m の灯標が基礎から 8.1 m 立っていて、ページは 6 m と呼ぶ
- **IALA の形状は意味そのもの**（can は左舷、cone は右舷、球は安全水域）。beacon にはそれが無いので
  schema が拒否する。受け取って描画で無視するのは、書いた人に「理解した」と言うのと同じ
- **浮標の記載位置はシンカーの位置。** 船は振れ回り円 `√(scope² − depth²)` の中のどこかに居る
  ——水深20m・scope 3 で 57m、参照事案の 49m / 121m の船に対して船1杯ぶんの遊び。
  どこに居たかは分からないので**動かさず、半径をパネルに出す**
  （`plans/done/antenna-offset-6.md` と同じ判断）
- **答えは3つある。** beacon は円が無い / mooring の無い浮標は円はあるが大きさが不明 /
  mooring のある浮標は数値。前2つを同じ空欄に畳むと、世界の事実とファイルの欠落が同じ顔になる
- **beacon は波に乗らない。** 基礎の上に建っていて海が横を流れる。`render/player.ts` の
  `float()` が種別で分岐し、`test/player.spec.ts` が「同じ1分を海面状態なしで走らせて
  完全に一致する」ことで固定している（曲率の沈み込みは残るので「動かない」では書けない）

- **灯質（rhythm）は標識そのもの。** IALA の方位標識は灯質以外で区別できない。文法は
  `core/light-character.ts` に1本だけ置き、マークごとの点滅表をデータに持たせない
  （`core/celestial.ts` と同じ形）。数値はすべて **IALA Recommendation E-110 Ed.4.0 (2016)**
  の Table 1〜3 から取る
- **略号は on/off の長さを決めない。** `Fl 4s` は「4秒に1閃」しか言わず、閃光の長さは
  その灯の灯台表にある。生成した列は **`inferred`** であって `measured` ではない
- **閃光率の帯（50〜79 / 80〜159 / 160〜300 回毎分）を信用し、PDF の周期欄を信用しない。**
  超急閃の欄は `0.5 s ≤ p ≤ 1.6 s` と読めるが 80/分は 0.75 s。帯は2箇所に書かれている。
  指定率（60 / 120 / 240）から組み立てると **Table 2 の worked example が全部再現する**
- **群間の暗を群内と同じにしない。** Fl(2+1) が Fl(3) に見える＝優先航路標識が普通の側方標識に
  化ける。群間は群内の3倍
- **読めない灯質を「それらしい既定」に落とさない。** Fl(2) を単閃に落とすと孤立障害標が
  特別標識になる。読めなければ何も光らせない
- **灯火の記載が無い＝無灯、ではない。** 浮標と灯浮標は別の標識で、報告書が灯火に触れないのは
  普通のこと。ここも3値（記載なし / 読めない / 読めた）
- **平面図で点滅させない。** 海図は瞬間ではない。灯火は船橋視点の夜だけに描く
- **ファイルが明示した点滅時間は略号と照合する。** 走る長さ・相の並び（配列長と明暗パターン）・
  色（両方向）・明暗比（比が級の定義）・役割ごとの長さ（長閃2秒、群間は群内の3倍、
  群内の率は級の帯、長点は短点の3倍）。**各相の長さそのものは照合しない**——それが明示する理由。
  役割は**生成側が phase に載せる**（照合側で生成規則を再実装すると2つが食い違う）
- **E-110 の「列に対する要求」は1つの validator に集約し、生成した列と明示された列の両方に
  当てる**（`core/light-character.ts` の `nonconformity`）。2つに分けると必ず食い違う——
  実際、明示 phase 側だけが閃光級に「暗 > 明」を厳密要求していて、**この工具自身が生成する
  E-110 の worked example（連続急閃 明0.5/暗0.5）を拒否していた**
- **この issue で出た指摘は27件、全部が同じ形だった**——「ファイルが言う灯質と絵に出る灯質が
  別物」。灯質は識別子なので、この不一致は見た目の劣化ではなく別の標識になる。
  一覧は `plans/done/light-character-41.md`

### 浮標は水面をなぞらない

- **上下動の固有周期は喫水だけで決まる**（`T = 2π√(d/g)`。水線面積が約分で消える）。
  だから**浮標では計算できて、船では計算できない**——船の横揺れは GM が要り、報告書に無い
  （#32 が保留な理由）。同じ論拠を浮標に借りてはいけない
- **利得だけでなく位相遅れも当てる。** 当てないと上下動と傾きが同じ瞬間に最大になり続け、
  「一様でない動き」の主要因（固有周期が違うので歩調が合わない）が消える。
  成分ごとに当てれば勝手に出る。共振で遅れは正確に 1/4 周期、それ以上では 1/2 周期へ向かうので
  **`atan` ではなく `atan2`**（`atan` だと折り返して「早い」になる——もっともらしく、逆）
- **スパーブイは直立を保つために存在する形。** 全部を局所傾斜に合わせると、
  その形が選ばれた理由そのものを消す。傾きの固有周期はバラスト（出典に無い）が要るので、
  **形ごとの階級**として持つ
- **遅れは位相に「足す」。** 進行波が `kx − ωt` なので位相は時間とともに**減る**——
  遅れて応答する物体は同じ位相に**遅れて**到達する＝位相は**大きい**。引くと共振の浮標が
  水面より 1/4 周期**先行**する（見た目は「波の中で動く浮標」のままで、動きが逆回り）。
  テストは**水面の頂と物体の頂の時間差を周期でくるんで**測る（算術の再計算では捕まらない）
- **「計算できる」は「計算した」ではない。** 周期は喫水だけに乗るので、喫水が記載されて
  いなければ（普通は無い）、形ごとの比率＝**この工具のモデル**が周期の全部を決める。
  `marks[].draughtMetres` は記載があるとき用で、パネルがどちらだったかを書く
- **減衰比はこの領域で唯一「出典の無い入力」。** 船体形状と heave plate の有無で変わる量で、
  「浮標の減衰比」という定数は存在しない。選んだ値としてパネルに書く。
  しかも共振で応答は `1/(2ζ)` なので、**この選択が一番効くのはモデルが一番信用できない場所**

### 標識は同じ事実を3回言う

方位標識は「黒上黄下」「上向きの円錐2つ」「Q」で3回「北」と言っている。**冗長性が設計**で、
縞は昼・頭標は色が読みにくいとき・灯質は夜のため。だから**フォーマットは意味を持ち、3つを
生成する**（`actors/mark/buoyage.ts`、IALA R1001 Ed 2.0 Table 1〜11）。3つを独立フィールドに
すると「黒黄の縞＋球2つ＋Fl(2)」という誰も識別できないキメラが書けてしまう。

- **日本は B 地域で、側方標識の色が逆**（赤が右舷、緑が左舷）。**地域は座標から導けない**
  ——境界は地図であって式ではない。記載が無ければ側方標識の色は生成しない
  （`meta.buoyageRegion`）。方位・孤立障害・安全水域・特殊は世界共通なので地域が要らない
- **色は1色では足りない。** 方位標識は全部帯、安全水域は縦縞、孤立障害は帯。`pattern` は
  種別＋順序付きの色で、**順序が意味**（黒上黄下は北、黄上黒下は南）
- **候補が複数ある灯質は「出典が周期まで書いているもの」を採り、選んだことをパネルが書く。**
  安全水域の4通りのうち周期が付いているのは LFl 10s だけ
- **側方標識と特殊標識は灯質を持たない**（「予約されたもの以外なら何でも」）。生成すると、
  何も識別しない灯を「識別できるように見える形」で水面に置くことになる
- **灯標の構造（塔・櫓・柱・杭）は意味を持たない。** 格子櫓の北方位標もある。浮標側の同じ軸
  である IALA 形状は**部分的に意味を持つ**（缶は左舷、円錐は右舷）ので、別のフィールドで
  別の種別に置く（#40 が種別を分けた理由）
- **出所は4値**: 記載 / 意味が一意に決めた / **意味が許す複数から選んだ** / 何も無いので選んだ。
  方位標識の色は黒上黄下しかないが、胴体は柱でも棒でもよく R1001 は決めていない——
  柱を「意味から」と報告するのは、**この工具の選択を IALA の権威の後ろに隠す**ことになる
  （発明した色を記載と呼ぶのと同じ種類の overclaim が1段内側に入っただけ）。
  `actors/mark/appearance.ts` が1か所で解決し、描画とページが同じ答えを使う

詳細は `docs/domain-notes.md` の「Sea marks」「The rhythm is the mark」「One fact, said three ways」。

### 「どういう画か」と「眼はどこか」は別の問い（#63）

`render/player.ts` はカメラの選択から画の種類を導いていた。

```ts
export type ViewKind = "overhead" | "bridge";   // 旧
const diagramMode = this.view.kind === "overhead";
```

**この真偽値 1 つに 8 つぶら下がっていた**——曲率、平面図グリッド、照明と地図の乗算色と空、
標識の点滅、航海灯の観客、灯の筋、出典表示、眼の有無。2 つの視点には全部正しいが、
**3 つ目には答えを持っていない**。

- **`Picture = "chart" | "world"` を視点から 1 度だけ求め、8 つに渡す。**
  各所で `view.kind === ...` を聞き直すと、聞いているのは画ではなくカメラになる
- **`Eye` から船を必須で外す。** `Eye` が `Cast` を持っていたので、
  **船に乗っていない眼は型として作れなかった**。`aboard: Cast | null` にする。
  `LampAudience` は既に 3 値（diagram / self / observer）で、**乗っていない眼は全船にとって
  observer**——型は既に用意できていて、できていなかったのは `Eye` のほう
- **自由視点カメラは船橋カメラを広げて作らない。** `placeBridgeCamera` は船首方位に向け、
  水平を保つ。**どちらも主張**（窓が向くのは針路であって進行方向ではない、
  当直者は水平線を見ている）。俯角引数を足すと、船橋視点がカメラワークまで 1 引数になる
- **枠決めは海図だけ。** `frameOverhead()` は地図タイルの取得範囲も決めるので、
  world で呼ぶと**誰も描いていない矩形の地図を取りに行く**
- **出典は「その画が出している地面の層」。** world は標高タイル。自由視点で淡色地図を出すと
  「海図を斜めから見た」という別の主張になる
- **割っただけで消費者を作らないと、割り方が正しいか誰も検証しない。**
  最小の自由視点を 1 つ載せて実証する（`prototypes/` を実装になったら消す規律の裏返し）

### 船橋視点では地球は平らではない

平面図は図面なので平らでよい。**船橋視点は違う。** 平らな平面の水平線はカメラの目線の高さ
（消失線）に出て、しかも何も水平線の下に沈まないので、20 マイル先の海岸が**水際線ごと**見える。
船を扱った人には一目で嘘——25 マイルで実際に見えるのは、麓の無い稜線だけ。

`core/horizon.ts` が算術（水平線距離・沈み込み・隠れる高さ）、`render/curvature.ts` が
当て方を持つ。**曲率は世界ではなく「視点」の性質**で、`setDiagramView` が照明と地図について
既に出している結論と同じ形（平面図は `uCurve = 0`）。

- **屈折を入れること。** 光は地球側へ曲がるので水平線は幾何学より遠い。係数 0.13、
  つまり半径 R/(1−k) = 7323 km で計算する。抜くと水平線が 7% 手前に来る
- **効くのは俯角ではなく沈み込み。** 眼高20mの俯角は 8′ で、55°の画角では2px 未満。
  沈み込みは 20 km で 27 m、50 km で 171 m
- **大きい面は頂点シェーダ、船は CPU で丸ごと。** 20 km 先の全長 180 m の船で船首と船尾の
  沈み量の差は 0.49 m（0.16°）。横傾斜も縦揺れもモデル化していないレンダラで、
  船を曲げるのは間違ったものへの精密さ。逆に**陸だけ沈めて船を沈めないのは駄目**で、
  それは前より強い主張をする画になる（`plans/done/antenna-offset-6.md` と同じ失敗）
- **曲率を当てるマテリアルのモデル行列は平行移動だけであること。** ローカル Y から引くので、
  回転や拡大が入ると鉛直方向でなくなる。`render/terrain.ts` がタイルを「寝かせた板」ではなく
  最初から世界の軸で組み立てているのはこのため
- **水面も一緒に曲げないと水平線が出ない。** `render/water.ts` は眼を中心にした等比リングの
  円盤で、リングを対数的に置くと水平線の近くだけ密になる。**水平線は描いていない**——
  沈んだ面が向こう側を隠すところがそれ

### 波を描くとき（#31）

- **ランダム性は足すものではない。** スペクトルから振幅を取り、**位相だけ**を乱数にする。
  振幅を乱数にすると分散が二重に入って Hs が合わなくなる
- **帯域をどこで切るかは宣言であって、収束する答えは無い。** 波数の2次モーメントは
  対数発散する。いまは **0.12 Tp**（Hs 3 m で帯域の下端 1.7 m、実際に出る最短成分は 1.9 m）。**Cox–Munk の実測（同じ海で 14.2°）
  には帯域を広げても届かない**——8 cm 波まで積分して 10° で、残りは表面張力波にあり、
  JONSWAP が記述する範囲でもレンダラが描ける範囲でもない。だから**描ける限界で切り、
  足りない分はページに書く**。#37 のグリッターは実測関係から幅を取る必要がある
- **描画の帯域は解析の帯域と同じにする。** 狭く描くと「有義波高は同じで交差回数だけ少ない海」に
  なり、パネルと画が同じ水について別のことを言う
- **成分は等エネルギーだけで割らない。半分をエネルギー、半分を傾斜密度で置く（#50）。**
  対数等間隔だと、エネルギーの無い低周波に大半を浪費して実質4本の正弦波になり、うなりが
  脈打つ（実測: 上位4本が分散の 79%。等エネルギーで 19%）。**等エネルギーはうねりの形には
  正しく、肌理には1桁間違う**——傾斜密度 `k²S` は `1/ω` でしか落ちないので、尾のどの1オクターブも
  同じ傾斜を持ち、どれもエネルギーを持たない。3 m の海で波長 23 m より短い成分は**1本**になり、
  その1本が傾斜の半分を担う——1波長・1方向の正弦波はチョップではなくトタン板（実際そう見えた）。
  `SLOPE_SHARE_OF_COMPONENTS = 0.5` は両方の測度を正規化して混ぜる宣言。実測で 1.7 m 以上の
  各オクターブに約4本、ピーク周りに19本、どの成分も分散の 5% 未満（うなりは戻らない）で、
  描画の rms 傾斜が 5.4° → 6.0°、帯域の 5.95° と一致する
- **向きを周波数と連動させない。** 波長順に向きを配ると「スケールとともに回る扇」になり、
  各成分が正しくても一目で嘘に見える
- **正しい波でもフレネル反射が無いと平らに見える。** Hs 3 m の rms 傾斜は 4°。拡散光だけでは
  画素が 10% しか動かない。昼の海が波に見えるのは**空の反射**
- **フラグメントの `normal` はビュー空間。** ワールド空間の傾斜をそのまま混ぜると、
  コンパイルも実行も通って海は平らなまま光る
- **メッシュは 150 m までしか幾何で波を載せられない。** 近傍は頂点変位、遠方は解析的な法線
- **波はシナリオの時計で動かす。** ただし uniform は float なのでエポック秒は渡せない
  （開始からの秒）。60倍速だと見かけの周期が 0.14 秒でストロボになるので、録画は速度を落とす
- **浮かぶものは「描かれた海」に乗せる。** 幾何がフラットに落ちた先で真の波高に乗せると、
  ブイが水面から浮く（#34）
- **帯域も「描かれた海」の一部。成分ごとに落とすなら、浮かぶものにも同じ帯域を渡す（#36）。**
  頂点シェーダは成分ごとにメッシュの粗さで落とすので、250 m 先の幾何は分散の 42% しか
  持っていない。CPU 側（`render/scene.ts` の `drawnSurfaceAt`）が全帯域を渡し続けると、
  **距離フェードではなく帯域の側から #34 と同じ「浮くブイ」が戻ってくる**。GLSL と TS の
  二重定義になるので、規則は `render/waves.ts` に隣り合わせで置く（Node はシェーダを
  コンパイルできない）
- **方向分散は Longuet-Higgins 形 `cos^2s(θ/2)`。** `cos^2s(θ)` と書くと同じ指数でも
  90° ずれた向きに重みが 1/4 残る。テストは公表の恒等式「平均合成長 = `s/(s+1)`」に当てる
- **波向は3段。** 記載の波向 ＞ 記載の風向 ＞ 名前の付いた仮定。風を足しても仮定は消えず、
  段が1つ増えて後退するだけ——**風が決めるのは風浪であってうねりではない**。パネルがどの段
  だったかを書く
- **ビューフォートは階級。** 風速が記載されていれば周期の導出に使うが、**風力階級だけなら
  範囲として運び、周期には使わない**（中央値を発明しない）。海面状態を潰さないのと同じ理由
- **風と海の突き合わせは非対称。** 完全発達は上限なので、記載の海が風の支える上限より
  大きいときだけ報告する。小さいのは普通（吹送距離と継続時間が足りていない）
- **その海を起こせない風から周期を取らない。** 無風＋2m のうねりは正当なファイルで、
  実際よくある（うねりは別の気圧系のもの）。そこから周期を取るとクランプに当たって 0.5 秒、
  つまり波長 0.4 m の 2m 海面になり、パネルは「風から導出」と書く。**もっともらしい表示の
  まま幾何が嘘**になる。起こせないなら波高側へ落とす
- **その判定は階級の「荒い側」で行う。** 導いた周期は階級の両端に当たるので、穏やかな側で
  判定すると通ってしまう（海面状態 4 と 14 kn で、1.44 m しか起こせない風が 2.5 m の海の
  周期を名乗る）。**警告は上げにくく、導出はどこでも成り立つ必要がある**——問いが違うので端も違う
- **却下の理由を真偽値の対で持たない。** 「風速が弱すぎた」と「階級に上限が無い」を1つの
  フラグに潰すと、**開区間で偽の文になる**（海面状態 9 に 150 kn は 16.6 m 起こせるので
  弱くない）。**間違った理由を名指すのは、理由を言わないより悪い**——読者に自分の数字を
  直させることになる。理由は網羅的な列挙で持ち、複数が真なら**より特定的なほう**を出す
- **スキーマで境界を付けたら、export された関数でもクランプし、両者をテストで結ぶ。**
  JSON と TS の二重定義で、どちらも相手を import できない。`validateScenario` を通っていない
  呼び出し側が export 関数に届く。**新しいフィールドを足すたびに、この規則を当て直す**
  （波高と周期で2反復かけて学んだのに、風速に足したとき適用しなかった）
- **「値が無い」を導く条件が、記載値まで飲み込まないこと。** 波高 0 の海に周期も方向も無い
  のは*導出できない*からであって、**ファイルが書いていれば別**——減衰したうねりは周期と
  波向を持ち、有義波高はほぼ 0 になる。記載の有無を先に見る。片方（周期）の不在を理由に
  もう片方（方向）まで隠さない
- **答えの無い問いにもっともらしい値を返さない。しかも直す場所を間違えやすい。**
  高さ 0 の海に周期は無いが、`periodFromWindSeconds(0)` を 0 にしても `seawayOf` が
  スペクトルの下限 0.5 秒に再クランプし、パネルは「0.5 s（風から導出）」と表示し続けた。
  **関係式を直すと嘘が1段下がるだけ**で、止めるべきは「そもそも周期が無い」と言える層
  （`periodFrom: "none"`）
- **階級の端は「数字か」を毎回問う。このレポで4回踏んでいる。** 海面状態 9 の上端（開）、
  ビューフォート 12 の上端（開）、海面状態 9 の周期導出、そしてビューフォート 0 の上端
  （**閉じていない**——「1 kn 未満」であって「0〜1」ではない。1 kn は既に風力 1）。
  端の条件は1か所に集める（`forceClass` / `seaStateClass`）。散らすと4回目を見逃す。
  **そして集めても足りない**——値を受け取る側が閉区間として読み直せば同じことが起きる
  （ビューフォート 0 を正しく「1 kn 未満」と持ちながら、注記だけが「階級の上端の 1 kn」と
  書いていた）。端を持つ値は、表示する層でも端を尊重する。
  **散文の検査は、分岐を1つ選ぶのではなく全部レンダリングして読む。** 8通り読んで4件出た
  （幅の無い階級を「範囲」と呼ぶ、波を1本も描かない海について「画から波向を読むな」と
  警告する、海面状態が無いのに「海面状態は方向を持たない」と説明する、波高 0 で
  「上位1/10 は 0.00 m」と書く）。**1つ読んで直すと、直した分岐だけが正しくなる**
  **さらに、直した文の隣がまだ間違っていることがある**——開区間の床を「rough end」と
  呼ぶのを1文で直したら、直後に連結される文が同じ値をそう呼び続けていた。
  **散文を直したら、その語をページ全体で grep する**（テストもページ全体で否定する）
- **風速と風力が併記されて矛盾していたら黙って捨てない。** 18 kn と BF9 はどちらかが誤りで、
  片方を黙って使うと読者は食い違いに気づけない。どちらが正しいかは決めず、あることを言う。
  **そのとき捨てた側の値も出す**——階級番号と範囲を見せないと、読者は食い違いを確認できず
  受け取るしかない（「示す」と言って入力の片方を隠している状態）

### 波は「平らに描く」が一番強い主張になる

平らな海はデータが無いときの穏当な既定ではなく、「相手はずっと見えていた」という主張で、
それは事故報告書がまさに争っている点。`plans/done/antenna-offset-6.md` と同じ形の失敗。

- **有義波高は上位1/3の平均**であって波の高さではない。上位1/10 は 1.27 倍、
  87分の最大は約 1.89 倍。水面の標準偏差は `Hs/4`
- **遮蔽を接地点1点の確率で出すと1〜2桁外す。** 視線のクリアランスは接地点のまわりで二次
  （`c(x) = A + (x−x*)²/2R`）で、1シグマ上がるまでに 2〜3 km ある。数百本の波が視線すれすれに
  立っていて、どれか1本でも越えれば隠れる。**線に沿って積分すること**
- **相手も波に乗るので、効く向きが距離で反転する。** 近距離では谷に落ちる方が効き、
  遠距離では峰に乗る方が効く。どちらも安全側ではないので**両方出して挟む**
- **入力が全部推定。** 海面状態の階級幅（SS4 は 1.25〜2.5 m）だけで答えが 54 倍動き、
  眼高と相手の高さは beam の何倍かで作った値（#8）。**単一値を出さない**

詳細と実測値は [`docs/domain-notes.md`](docs/domain-notes.md) の「海」。

### 水面が返すのは色ではなく「向き」（#37）

反射は波を見せているだけでなく、**空そのもの**。1色を返している限り、反射は模様であって
証拠ではない。

（#37 の時点では水面が空の全部だった——ドームは無く、天体は反射の中にしか出なかった。
**いまは違う**: #53 で水平線の上にも同じ勾配関数からドームを描いている。下の #53 の節を参照）

- **光の道（glitter path）は方位を持つ。** 道の上にいる相手は道を背にして見えるか紛れる。
  位置は天体の方位（計算値）、幅は**傾斜 rms の 2 倍**（法線が θ 傾けば反射光線は 2θ 曲がる）
- **幅は Cox–Munk の実測から取り、描かれた海の傾斜からは取らない。** #36 と #50 の後でも
  描画 6.0° 対 実測 14.2° で、描かれた法線だけでは道の幅が 1/3 になる——**実在しない鋭さの海**。
  足りない分散を天体側のローブに入れる。**描けない粗さを陰影で表すのは標準的なやり方だが、
  宣言する**
- **「幅」は 3 つあり、取り違えると 1.4 倍太い道になる（一度踏んだ）。** `mss` は**傾斜 2 成分の
  合計**で、rms 傾斜は `sqrt(mss)`（3 m の海で 14.2°）。「rms 傾斜の 2 倍」（28.3°）は
  **2 次元での特性半径**。Gaussian のローブが要るのは**1 軸方向の標準偏差** `sqrt(2·mss)`
  （20.4°）。ページに出すのは**半値全幅**（48°）——読者が画面と突き合わせられる唯一の量
- **海が記載されていなければ道を描かない。** 幅が計算できず、鏡のように鋭い天体は
  「べた凪だった」という主張になる
- **明るさは上界。** 雲を誰も書いていないし、レンダラは測光的に較正されていない。
  **比だけが誠実**（位相間・天体間）。**照輝率で線形にスケールしない——半月は満月の 1/11**
  （衝効果。Krisciunas & Schaefer 1991 の位相則を使う）
- **露出は条件ごと（#55）。** 夜空 0.003 と昼空 0.60 は 200 倍違うので、**1 つの定数では
  両方を賄えない**。共用していたときの実測は「昼の道の中心が 3.1（1.0 が白）、中心から 25.5°
  まで真っ白」——道ではなく穴。`ambient` は最初からパレットごとに別で、理由も同じ。
  **比は計算値のまま、絶対値だけがパレットの一員**（`bodyLobe` / `streak`）
- **空と key light は同じ 1 つの方向から駆動する。** 水面が方位 191° の月を返しながら船体が
  別方向から照らされるのは、同じフレームで 2 つのことを言うこと（#15 の一部をここで持っていった）
- **どちらの天体が照らすかは「絵」に従う。** レンダラは記載の `lightCondition` で夜／昼を
  描くので、記載が夜で太陽が地平線上（＝日付かタイムゾーンの転記ミス）のとき、水面が太陽を
  返してはいけない。**それは食い違いを絵で報告することになり**、パネルは文で報告している
- ローブは Gaussian なので**裾がある**。同じ高度で方位が 180° 違う 2 点は 98° しか離れておらず、
  σ=26° では peak の 1/1000 が残る。peak を大きく取ると**空全体が持ち上がる**（一度踏んだ）

`core/illumination.ts` は #37 で書かれた（段階3）。物理はそこ、近似は `render/sky.ts`。

- **空は「水面が返すもの」と「水平線の上」の 2 か所に、同じ 1 つの関数から描く（#53）。**
  #37 は水面にだけ勾配を入れ、しかも `palette.sky` の意味を「空の色」→「**水平線の色**」に
  変えて淡くしたので、**画面の上半分が勾配の一番淡い端で塗りつぶされた**（霞んで見える）。
  画角 55° の船橋視点は画面上端が水平から 27° 上で、そこは晴天なら水平線よりはっきり濃い
- **勾配は 1 つの関数、天体は別。** 水面はグレージング角でほぼ「水平線のすぐ上の空」を返すので、
  勾配を 2 つ定義すると**水平線に継ぎ目が出る**。一方、天体の幅は海が広げるもので、
  空では視直径のまま——だから幅は呼び手が決める
- **「海の記載が無ければ天体を落とす」門は水面のもの。** 月は海の記載と無関係に空にある。
  空のシェーダにこの門を持ち込まないこと
- **ドームには霧を当てない。** `buildFog` の far は視程が無ければ表示範囲の 3 倍なので、
  霧に入れるとどのシナリオでも空が霧色 1 色に戻る。ただし
  **霧の上に晴れた空が見えるのは霧ではない**ので、パネルがそう書く

### 灯火が水面に敷く筋（#39）

天体と**同じ機械で、距離を入れ直しただけ**。天体は無限遠なので方向だけ比べるが、灯は有限なので
`normalize(灯 − そのフラグメント)` と比べる——それが鏡面条件そのもので、筋が
「眼の足元と灯の足元の間に伸びる」のは帰結として勝手に出る。広がりは**海のもの**なので
`render/sky.ts` の `lobeWidth` を共有する（2 つ書くと月と舷灯が別の海で反射する）。

- **灯は水面に 2 つのことをする。反射だけ描くと「こちらにしか光らない」灯になる（#58）。**
  **筋（鏡面）**は原理的に方向を持ち、鏡面条件が成り立つ線の上にしか出ない——正しいが半分。
  **光だまり（拡散）**は灯が水面を照らしたもので、**どの方位からも見える**。
  弧が光だまりの形を決めるので、**全周灯は円、舷灯は 112.5° の楔**——第21条が水面に見える形。
  拡散は反射ではないので **Fresnel を掛けない**（Schlick は「鏡面方向の向こうから何が返るか」の話）
- **弧は「水面の点」で判定する。** 灯は自分の弧の中しか照らさない。眼で判定すると、
  正横後方から見た船の**船首側の水面に紅い筋**が出る（`visibleLights` と同じ 180° の罠）。
  **自船の灯も水面を照らす**——`showFor` は自船の灯を隠すが、それは「灯そのものが見えない」
  からで、水面への効果は見える
- **灯の光度は「計算できない」ではなく計算できる（#58）。ただし出るのは下限（#59）。**
  第22条は距離の下限しか言わないが、**附属書 I 第8節がその距離と光度の関係を与える**:
  `I = 3.43×10⁶ · T · D² · K^(−D)`（T = 2×10⁻⁷ lx、K = 0.8/NM）。
  マスト灯 6 NM → **94 cd**、舷灯 3 NM → **12 cd**。
  **第8節の表題の "minimum" は効いている。** これは「基準をぎりぎり満たす灯」の値で、
  実物はそれ以上のどこか。附属書は「最大光度は過度のグレアを避けるよう制限すべき」と
  第8節末尾の Note で言うだけで**数値を与えていない**ので、上側は規則からは決まらない。どこかは
  事故報告書に書かれていない。だから **94:12 は 2 つの灯の比ではなく 2 つの法定下限の比**。
  識別子も `minimumCandela` / `minimumCandelaForRange` にしてある
  （`ASSUMED_MARK` / `CHOSEN_DAMPING` と同じ流儀で、出所を名前に書く）
- **垂直配光はそれより弱い保証で、しかも水面を照らすのはそちら側（#59 で 2 回間違えた）。**
  第10節が定めるのは**帯ごとの下限**——±5° で所要光度以上、±7.5° で 60% 以上——で、
  **7.5° より下は何も要求していない**。描いている曲線は 5° までは下限に一致するが、
  **6° で 0.815 を描く（保証は 0.60）**ので適法な灯より明るいことがあり、7.5° より下は
  そもそも下回る下限が無い。
  **そして俯角は距離とともに小さくなるので、帯は水面上では「距離の範囲」になる。**
  灯高 20 m のマスト灯なら 7.5° が 152 m、5° が 229 m:

  | 水面 | 俯角 | 描画値の性格 |
  |---|---:|---|
  | 152 m 以内 | 7.5° 超 | ツールの裾。下に規則が無い |
  | 152〜229 m | 7.5〜5° | 60% 下限より上。適法な灯はもっと暗くてよい |
  | 229 m 以遠 | 5° 未満 | 下限そのもの＝実灯に対する下界 |

  **明るいところほど規則が無い。** ピークは 78 m（俯角 14.4°）、よく引用する 100 m は
  11.3°、どちらも 152 m の内側で、ピークは**全光度の下限が始まる** 229 m の 3.4 倍
  （60% の下限は 152 m から既に効いている）。
  **だから「目につく光だまりはツールの曲線、遠くの淡い水面は下限」と距離で分けて書く。**
  「全部下限だから少なくともこの明るさ」も「全部無規定」も overclaim で、
  #59 で**両方向に 1 回ずつ**やった
  水面の照度は `E = I·cosθ/d²` ＝ 水平な海なら **`I·h/d³`（3 乗）**。
  ただし **0.0018 lx は「ビームを水面に向けたら」の値で、実際に描くのは垂直配光を入れた
  0.00049 lx**（星明かりの 1/4、参照事案の月 0.018 lx の 1/40。ピークは 78 m 先の
  0.00053 lx）。**上限のほうを引用すると絵より 4 倍明るく報告することになる**——
  ページ・ドメインノート・テスト名の 3 箇所で実際にそうなっていた（#59）。
  **水平距離の 2 乗で書くと**（一度そう書いた）
  **自船のマスト灯が数百 m 先まで海を照らす**。宣言として残るのは
  「1 lx を画面のいくつに写すか」の 1 つだけで、満月の 0.25 lx が天体の露出に乗るよう選ぶ
  ——**月と灯が同じスケールに乗る**
- **航海灯は水平ビームの器具（附属書 I 第10節）。点光源として扱うと自船の足元の海を照らす。**
  光度が保証されるのは**水平から ±5°**、±7.5° で 60%、その下は規定が無く実機は急落する。
  結果、**水面の照度はビームが水面を掠める辺り（マスト灯 20 m で 100 m 前後）で最大**になり、
  **真下はほぼ 0**。2 点は規則のもので、その間と下の形は宣言
- **筋も光だまりも「その水面片に届く光」でスケールする**（灯の光度 ÷ その点までの距離²
  ＝点光源のグリッター放射輝度）。違いは**入射余弦を取るのは光だまりだけ**——鏡はどれだけ
  斜めから来たかを気にしない。**眼までの距離でスケールすると、どの灯も同じ明るさの
  レーンを 1 本ずつ引き、船の 3 灯が 1 つに見える**（一度やった。灯の足元が明るいのを
  直そうとしたが、原因は垂直配光の欠落のほうだった——1 つの不具合に 2 つの直しを当てて壊した）
- **シェーダにしか無い規則は TS に写しを置いて測れるようにする（#58 で 3 回間違えた）。**
  灯の寄与は `lampLight` が GLSL の写しで、テストが「灯ごとにレーンの位置が違う」ことを固定する。
  **理屈では 3 回とも間違えた**（眼までの距離でスケール／垂直配光の欠落／ビームの俯角を
  facet の入射余弦から取る——波が傾くとビームが自分の方へ向く）。測って初めて出た
- **筋は灯より先に消える。** 反射は光源より暗いので、灯より遠くまで見える筋は**検出の発明**。
  第22条はカンデラではなく距離の下限しか与えないので、**不等式を宣言して強制する**
  （公称距離の半分で 0）。半分は選択、1 未満であることは選択ではない。
  **判定は「灯→水面→眼」の全経路で行う**——反射光線は三角形の 2 辺、直射は 1 辺なので、
  全経路は灯‐眼距離より短くなり得ない。**灯‐水面の 1 辺だけで測ると**（素直な書き方）、
  灯の真下の水面が 3 マイル灯の 4 マイル先の眼へ筋を届ける
- **ただし全経路で測ってよいのは筋だけ。光だまりに眼の脚を入れてはいけない（#59 で踏んだ）。**
  光だまりは灯が水面に落とした光そのもので、**観測者はその中に居ない**——誰がどこに立とうが
  水面に届く光は変わらない。カットも減衰も**灯‐水面の 1 辺**で行う。全経路で減衰させると
  **カメラを動かすだけで海面の照度が変わる**: 6 NM マスト灯の 200 m 下の水面が、眼が
  真横のときと 3 km 離れたときで **84 倍**変わり、**5.4 km で完全に消える**（灯自体は
  11.1 km まで見える）。**同じ 1 つの `fall` を両方に掛ける**のが素直な書き方で、それが罠
- **標識の筋は灯質を運ぶ。** `Q(9)` の下の定常の筋は、筋を描かないより悪い主張
  （灯質が標識の identity そのもの）。`showingAt` を灯と同じ瞬間に聞く
- **入りきらない灯はページが言う。** シェーダの配列長は定数。黙って落とすと
  「灯を描いた」と書くページと、無い画になる

### 標高タイルは淡色地図と同じ仕掛けで、1つだけ違う

配置は淡色地図とまったく同じ（タイルごとに四隅を `toLocalPosition`）。違うのは
**画素を読むこと**で、そのため canvas が汚染されていてはいけない。地理院タイルは
`Access-Control-Allow-Origin: *` を返すので通る——これが無ければこの方式は成立しなかった。

- **PNG 形式は `dem_png/*.png`。** `dem/*.txt` は同じ数字で 7 倍のバイト数
- **海はタイルが無い（404）。** これが安さの理由。「エラー」ではなく通常の答え
- **無効値は (128,0,0)。** 内陸の欠測も同じ値なので、「無効＝海」と読むと山に穴が開く
- **距離でズームを落とし、前方の楔だけ取る。** 固定ズームで全周を舐めると桁が変わる。
  参照事案で全周 3.9 MB、90°の楔で外洋向き 949 kB・海岸向き 3.09 MB
- **取り直しはタイマーではなく移動距離で決める。** 再生中の眼は止まらないので、
  `basemap.ts` の settle タイマーはここでは永遠に発火しない

### 地図タイルは Web Mercator、シーンはローカル ENU

タイルを1枚の板にまとめて一律スケールで貼ると合う気がするが、合わない。緯度 33.9 の
縮尺係数は 1/cos = 1.2048 で、**南北 22 km で 40 m ずれる**——海岸線が船の全長より大きく動く。
**タイルごとに四隅の緯度経度を `toLocalPosition` に通して置くこと。** そうすると残るのは
タイル内部の歪みだけで、z=13 で 7.3 m、z=14 で 3.6 m、z=15 で 1.8 m。出典データの精度
（0.1 秒 ≒ 3 m）の下に収まる。`test/basemap.spec.ts` が「行と行が隙間なく接する」ことで
固定している——期待座標と突き合わせると、実装が間違っているときテストも同じだけ間違う。

タイルの **y は北から数える**（(0,0) が世界の左上）。逆に読むと、箱が縦に長いときだけ
地図が上下逆になる。そして **y は緯度に対して線形ではない**（メルカトルなので対数）。

**地図は実行時に取りに行き、焼き込まない。** 地理院タイルはリアルタイム表示なら出典明示のみで
申請不要、動画の背景も不要だが、保存すれば複製で扱いが変わる。容量も別物（この事案 z=15 で
base64 41.6 MB）。`build:single` の「外部参照ゼロ」は**ページが開くために要るもの**の話で、
地図が来なくても再現はそのまま動く。詳細は `src/render/basemap.ts` の冒頭。

**出典は canvas の中に描くこと。** 録画は `canvas.captureStream()` なので、canvas の外にある
DOM は動画に1フレームも映らない。`src/render/overlay.ts` が2パス目で描く（下の時刻と同じ仕掛けで、
出典専用のモジュールは無い）。そして **タイルが1枚も届いていないうちは出さない**
（日本の外では 404 になる）。**地図を映していない船橋視点でも出さない。** この2つの判定は
`render/player.ts` が持っている——`onFirstTile` で立つ `mapCredited` と、平面図かどうかの積。
出ていない地図の出典を書くのは、幾何ではなく散文の側で嘘をつくことになる
——`plans/done/antenna-offset-6.md` と同じ失敗。

**ズームを固定しないこと（一度踏んだ）。** 平面図は数十 km から数百 m まで 100 倍以上動く。
固定範囲を1回取ってフェードで誤魔化すと「中くらいのスケールでだけ地図が出る」になる。
フレームからズームを選んで取り直す。**判定にズームを含めること**——寄る方向では画面は
取得済み範囲から出ないので、「範囲内か」だけ見ていると最初の段に張り付く。そして
**次の段が1枚届いてから前の段を捨てる**（先に捨てると 2 の冪をまたぐたび地図が消える）。

**地図の乗算色を light condition で変えないこと（一度踏んだ）。** 淡色地図は陸と海の明度差が
1割程度しかないので、夜に合わせて暗く落とすと差ごと消えてただの黒い染みになる。**夜に見える
ほど暗い＝読めないほど暗い**で、この2つは同じ量の暗さ。平面図は図面であって夜ではない
——`setDiagramView` が照明について既に出している結論と同じ。`MAP_TINT` は1つだけ（`0xe6ecf0`）。
船橋視点は夜のまま（あちらは暗さ自体が証拠）。

**代わりに時刻を画の中に出す。** 上の変更で「平面図が暗い＝夜の事案」という手がかりを捨てるので、
読み取れる形で戻す必要がある。`render/overlay.ts` が右上に日付と時刻、右下に地図の出典を、
**canvas の中に**描く（録画は `captureStream()` なので外にある DOM は1フレームも映らない）。
時計は等幅（`figures`）で描くこと——プロポーショナルだと桁が変わるたびに幅が動いて、
毎フレーム geometry を作り直すことになる。

### 航跡の開始・終了時刻は揃わない

2隻の再現で、両方の記録が同じ時刻に始まることはまず無い。参照事案では **B の AIS 記録が
A より 30 分遅れて始まる**（A 16:50:25 / B 17:20:01）。

**画角を「いま航跡がある船」だけで決めると、その 30 分ぶん（20倍速で 90 秒）フレームが
1隻に張り付く。** 2隻が海の両端から近づいてくるのを見せるはずの区間がそれで潰れる。
`boundsToHold()` は、航跡がその瞬間に届いていない船には**一番近い時刻の位置**を聞く
（開始前なら最初の報告位置、終了後なら最後の報告位置）。これは画角の話であって船の話ではない
——船自体は自分の記録が始まるまで隠れたままで、その空間を埋めるのは航跡線
（平面図は航跡を全長描く）。

この事案では、再生開始時の画角が 77 km になって両船が入る。

### 直線補間は暫定であって、正しくない

`sampleAt()` は今のところ標本間を直線で結んでいる。船はそう動かない——舵を切っても
すぐには回らず、回り始めたら惰性で回り続け、船首は進行方向から偏角ぶんずれる。
1分間隔の標本を直線で結ぶと、船が横滑りしているように見えて、船を扱ったことがある人には
一目で嘘だと分かる。**ここを一次遅れ（野本）モデルに置き換えるのが次の仕事。**
それまで、合成した点はすべて `derivation: "interpolated"` として返している。

### PDF から取るとき

- **全角文字**。`ＡＩＳ` `ＶＨＦ` `ｋｎ` は全角。半角 `AIS` で grep すると 0 件になる
- **列見出しを信用しない**。`pdftotext` のレイアウト復元は見出し行をずらす。ある報告書は
  見出しが「船首方位」に見えたが、実際の値域は 1.8〜11.3 で衝突後にゼロへ落ちた——速力だった。
  **列の意味は値域で判定する**
- **正規表現の `\s` は改行を跨ぐ**。行末の数値列を `(?:\s+\d+)+` で取ると次の行の時刻を食い、
  行を1つおきに取りこぼす。半分になっても表として成立して見えるので気づきにくい。
  行内の空白は `[ \t]` で書くこと

### 許容差は区間長に応じて広げる

報告書の緯度経度は秒まで（行によっては 1 秒刻み）。経度 1 秒は中緯度で約 26m なので、
20 秒間隔の 2 点では丸めだけで 1kn 以上ずれる。固定の許容差だと短い区間で必ず誤検出する。
`checkPlausibility` は `positionQuantisationMetres / 区間秒数` を許容差に足している。

## 航跡の線は2つのことを別々の手がかりで言う

1本の線に2つの主張が乗っている。**混ぜないこと。**

| 手がかり | 何を言うか |
|---|---|
| 実線 / 破線 | その数字が**どこから来たか**（記録 / 推定）。JTSB の航行経路図と同じ約束 |
| 濃い / 薄い | その時刻に**もう通ったか**（走行済み / これから） |

「まだ走っていないところを破線に」は素直だが**やってはいけない**。推定区間のあるシナリオで
2つの意味が同じ線に着地して、どちらも読めなくなる。参照事案は全区間 measured なので、
破線を1本も見ないまま作ると気づかずに踏む。

走行済みと未走行の継ぎ目は**その瞬間の船の位置**であって、最寄りの標本ではない。
標本間隔 1 分は 0.5 マイル近くあるので、そこで線の濃さが変わると船から離れた場所を指してしまう。
`splitAt()` が `sampleAt` の補間位置で継いでいる（`src/render/scene.ts`）。

## 出所（derivation）は飾りではない

各点が `measured`（記録）/ `digitised`（航跡図から読み取り）/ `inferred`（口述から復元）/
`interpolated`（このツールが合成）のどれかを持つ。**これがこのツールが「再現」を名乗れる
根拠そのもの**なので、レンダラも推定区間は実測と描き分けること（破線・半透明など）。
JTSB 自身が航行経路図で実線と破線を描き分けているので、これは発明ではなく踏襲。

## examples/ は仕様のテストでもある

`examples/*.voyage.json` は CI が `spec/voyage.schema.json` で検証する。スキーマを変えたら
例も直る必要がある。**例が通らなくなったら、それは書き留められていないフォーマット変更**で、
ユニットテストには見えない唯一の壊れ方。

`examples/suo-nada-2025-11-27.voyage.json`（周防灘の衝突、実データ 59 点）は参照事案で、
`test/examples.spec.ts` が最接近距離・方位の不動・灯火の見え方まで固定している。
このファイルの数字を触るときは、報告書の付表と突き合わせること。

## issue と plans/ の使い分け

**issue が入口、`plans/` は設計判断の置き場**。二重管理にしないため、番号で結ぶ。

1. **issue を先に立てる。** 外から見え、番号が採れる
2. **設計判断が要るものだけ `plans/<slug>-<issue番号>.md` を書く。** 小さい修正は issue だけでよい。
   何を・なぜ（特に「素直な実装だと何が間違うか」）・どのファイルを触るか・未解決の問い
3. **実装 PR に `Closes #N` を書き、merge したら plan を `plans/done/` へ移す**

issue のコメント欄ではなく `plans/` に置く理由は、**未解決の問いがコードと一緒に動いて
PR でレビューされる必要がある**から。`plans/ship-motion-model.md` の「係数の出どころ」が例で、
これは実装のたびに読み返される前提になっている。逆に「誰がいつやるか」「外部との議論」は
issue の仕事で、`plans/` に持ち込まない。

移し忘れは実際に起きる（`phase1-video.md` は実装後もしばらく `plans/` に残っていた）。
本数が増えて手で回らなくなったら、`gh pr view <num> --json mergedAt` で実値を取って
triage する形に自動化する。**完了日を推測で書かないこと。**
